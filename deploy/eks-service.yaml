apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentzarvis-api-ingress
  annotations:
    kubernetes.io/ingress.class: alb
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agentzarvis-api-svc
            port:
              number: 80
